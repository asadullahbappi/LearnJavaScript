// Logical || Statement operators

//Example 1:
let age = 5;

if (age < 6 || age > 65) {
	console.log('You get in for Free');
} else {
	console.log('You have to Pay');
}

//Example 2:

let color = 'RED';

if (color.toLowerCase() === 'blue' || color.toLowerCase() === 'yellow' || color.toLowerCase() === 'red') {
	console.log('Great choice');
} else {
	console.log('Not a Great Choice!');
}
