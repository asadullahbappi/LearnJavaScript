// Not Operator

//Example 1:

let loggedInUser = 'asad';

if (!loggedInUser) {
	console.log('You are Logged In!');
} else {
	console.log('You are not Logged In!');
}

//Example 2:

let flavor = 'water Melon';

if (flavor !== 'grape' || flavor !== 'cherry') {
	console.log('We do not have that falvor');
}
